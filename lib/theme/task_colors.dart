import 'package:flutter/material.dart';

//addetional colros will be used not in themeeeeeeeas
const Color low = Color(0xFF80FFA3);
const Color medium = Color(0xFFFFD070);
const Color high = Color(0xFFFF8080);
